const initialState = {
  loading: false,
  error: null,
	success: false
}

const cortege = (state = initialState, action) => {
	switch (action.type) {
    default: 
			return state;
	}
}

export default cortege;
